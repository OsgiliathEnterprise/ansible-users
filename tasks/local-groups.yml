---

- name: ansible-users | local-groups | manage local-group with robertdebock role
  include_role:
    name: robertdebock.users
  vars:
    users_group_list: "{{ users_group_list2 | json_query(\"[?!freeipa]\") | sort | list }}"
    ansible_become: true